# Codex Example

This example targets repos that use `AGENTS.md` as the primary instruction surface.

Typical flow:

```bash
./bin/memorymesh init
./bin/memorymesh query "What should an agent know before editing this repo?"
```
