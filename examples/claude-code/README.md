# Claude Code Example

This example shows the file surfaces MemoryMesh imports and exports for Claude Code style repos:
- `CLAUDE.md`
- `.claude/`

Typical flow:

```bash
./bin/memorymesh init
./bin/memorymesh serve
```

Use the MCP config snippet printed by `memorymesh init`.
