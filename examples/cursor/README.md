# Cursor Example

This example targets repos that use `.cursor/rules/` as part of their instruction surface.

MemoryMesh imports those files during bootstrap and can export reviewed skill packs back into file-based profiles later.
