# Bench Harness

The benchmark harness is intentionally simple and reproducible.

It should answer:
- How long does bootstrap take?
- How fast is lexical search on warm data?
- What answer quality do we get for a fixed query set?
- Does data survive failure and restart scenarios?

Run:

```bash
bun run bench
```
